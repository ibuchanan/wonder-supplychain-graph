/**
 * Reserved Forge entry point.
 *
 * SCG starts with a pure collaboration core. The initial issue panel is UI-only;
 * backend handler exports will be added only when their contract is required.
 */
export {};
