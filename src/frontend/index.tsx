import React from "react";
import ForgeReconciler, { Text } from "@forge/react";

const App = () => (
  <Text>
    The Supplychain Graph collaboration core is being prepared for this Epic.
  </Text>
);

ForgeReconciler.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
