modules:
  graph:connector:
    - key: scg-package-connector
      name: Supplychain Graph Package
      capabilities:
        replicatesPermissions: false
        syncFidelity: mirror
        supportsIncrementalSync: false
      icons:
        light: https://developer.atlassian.com/platform/forge/images/icons/issue-panel-icon.svg
        dark: https://developer.atlassian.com/platform/forge/images/icons/issue-panel-icon.svg
      objectTypes:
        - atlassian:work-item
      datasource:
        onConnectionChange:
          function: scg-connection-change
  jira:issuePanel:
    - key: scg-epic-panel
      resource: main
      render: native
      title: Supplychain Graph
      icon: https://developer.atlassian.com/platform/forge/images/icons/issue-panel-icon.svg
  jira:globalPage:
    - key: scg-simulator
      resource: simulator
      render: native
      title: Supplychain Graph Simulator
  function:
    - key: scg-connection-change
      handler: index.onPackageConnectionChange
    - key: publishWorkPackage
      handler: index.publishWorkPackage
    - key: publishPackageToGraph
      handler: index.publishPackageToGraph
  automation:actionProvider:
    - key: scg-publish-package-provider
      actions:
        - scg-publish-package
        - scg-publish-package-graph
  action:
    - key: scg-publish-package
      name: Publish work package
      function: publishWorkPackage
      description: Queues a governed work-package publication for a Source Epic.
      inputs:
        sourceEpicId:
          title: Source Epic ID
          type: string
          description: The Source Epic key, for example {{issue.key}}.
          required: true
        publisherId:
          title: Publisher account ID
          type: string
          description: The Automation publisher account ID.
          required: true
        correlationId:
          title: Correlation ID
          type: string
          description: An Automation-run correlation ID.
          required: true
        idempotencyKey:
          title: Idempotency key
          type: string
          description: A stable Automation-run idempotency key.
          required: true
      actionVerb: CREATE
    - key: scg-publish-package-graph
      name: Publish package to Rovo demo
      function: publishPackageToGraph
      description: Promotes and indexes the current demo work package for Rovo discovery.
      inputs:
        connectionId:
          title: Graph connection ID
          type: string
          description: The Supplychain Graph connector connection ID.
          required: true
        sourceEpicId:
          title: Source Epic ID
          type: string
          description: The Source Epic key, for example {{issue.key}}.
          required: true
        publisherId:
          title: Publisher account ID
          type: string
          description: The Automation publisher account ID.
          required: true
        correlationId:
          title: Correlation ID
          type: string
          description: An Automation-run correlation ID.
          required: true
        idempotencyKey:
          title: Idempotency key
          type: string
          description: A stable Automation-run idempotency key.
          required: true
        publishedAt:
          title: Publication time
          type: string
          description: The ISO-8601 timestamp to show for the published package.
          required: true
      actionVerb: CREATE
app:
  runtime:
    name: nodejs24.x
    memoryMB: 256
    architecture: arm64
  id: ari:cloud:ecosystem::app/5c6de3cf-cb86-4873-8621-3f28f2537757
permissions:
  scopes:
    - delete:object:jira
    - read:object:jira
    - storage:app
    - write:object:jira
resources:
  - key: main
    path: src/frontend/index.tsx
  - key: simulator
    path: src/frontend/simulator.tsx
